{
  "name": "@tms/auth",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test test/*.test.ts"
  },
  "dependencies": {
    "@tms/tenancy": "workspace:*",
    "@tms/security": "workspace:*",
    "jose": "^6.1.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
